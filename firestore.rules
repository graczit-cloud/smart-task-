rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through Firebase Functions (backend), not direct client access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
